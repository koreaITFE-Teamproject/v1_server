<!DOCTYPE html>
<html lang="en">

<head>
  <title>컬럼 목록</title>
  <link rel="stylesheet" href="/column/list.css" />
  <script src="/column/test.js"></script>
  <script src="/column/column.js"></script>
</head>

<body>
  <div class="path-wrap" id="path-wrap">
    <p class="path"> <a href="/main/home"><i class="fa-solid fa-house"></i></a> >
      <span>칼럼</span> > <span id="change-path"></span>
    </p>
  </div>

  <article class="column-list-article">
    <!-- 버튼 -->
    <div class="option_bar">
      <ul>
        <% if(!isLogined.isLogined) { %>
          <li>전체 칼럼</li>
          <% } else { %>
            <li>전체 칼럼</li>
            <!-- 구독한 작가 칼럼 삭제 해야함, 구독 기능없앰 -->
            <li>구독한 작가 칼럼</li>
            <li>좋아요 한 칼럼</li>
            <% } %>
      </ul>
    </div>

    <!-- 컬럼 검색,글쓰기 버튼 -->
    <div class="column-search-wrap">
      <input id="col-type" type="hidden" value="<%= !type ? '' : type %>">
      <select id="column_type">
        <% var options=['==선택==', ' 국내 소설' , '해외 소설' , '시/에세이' , '인문' , '가정/육아' , '요리' , '건강' , '취미/실용' , '경제/경영'
          , '자기계발' , '정치/사회' , '역사/문화' , '종교' , '예술/대중문화' , '기술공학' , '외국어' , '여행' , '컴퓨터/IT' , '잡지' , '만화' ]; for(var
          i=0; i < options.length; i++){%>
          <% if(!type) { %>
            <option value="<%= i %>" <%=i==0 ? "selected" : "" %>>
              <%= options[i] %>
            </option>
            <% } else { %>
              <option value="<%= i %>" <%=i==type ? "selected" : "" %>>
                <%= options[i] %>
              </option>
              <% } %>
                <% } %>
      </select>
      <input class="search-input" id="search-input" type="text" placeholder="검색창" />
      <button class="search-column" id="search-column">검색</button>
      <button class="add-column-btn" id="add-column-btn" onclick="location.href='/column/write'">글쓰기</button>
    </div>

    <!-- 게시글 목록 요소 -->
    <div id="postBody"></div>
  </article>
  <nav aria-label="Page navigation example" class="pagination-container">
    <ul class="pagination"></ul>
  </nav>

</body>

</html>