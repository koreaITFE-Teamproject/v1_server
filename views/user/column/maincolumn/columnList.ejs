<!DOCTYPE html>
<html lang="en">

<head>
  <title>컬럼 목록</title>
  <link rel="stylesheet" href="/column/list.css" />
  <script src="/column/test.js"></script>
  <script src="/column/column.js"></script>
</head>

<body>
  <div class="path-wrap">
    <p class="path"> <a href="/main/home"><i class="fa-solid fa-house"></i></a> >
      <span>칼럼</span> > <span id="change-path"></span>
    </p>
  </div>

  <article class="column-list-article">
    <!-- 버튼 -->
    <div class="option_bar">
      <ul>
        <% if(!isLogined.isLogined) { %>
          <li>전체 컬럼</li>
          <% } else { %>
            <li>전체 컬럼</li>
            <li>구독한 작가 컬럼</li>
            <li>좋아요 한 컬럼</li>
            <% } %>
      </ul>
    </div>

    <!-- 컬럼 검색,글쓰기 버튼 -->
    <div class="column-search-wrap">
      <input class="search-input" id="search-input" type="text" placeholder="검색창" />
      <button class="column" id="search-column">검색</button>
      <% if(isLogined.isLogined) { %>
        %><button class="add-column-btn" id="add-column-btn" onclick="location.href='/column/write'">글쓰기</button>
        <% } else { %>
          <button class="add-column-btn" id="add-column-btn" onclick="alert('로그인 후 이용 가능합니다.')">글쓰기</button>
          <% } %>
    </div>

    <!-- 게시글 목록 요소 -->
    <!-- 개시글 요소 생성시 -->
    <table id="postTable">
      <thead>
        <tr>
          <th>No</th>
          <th>제목</th>
          <th>글쓴이</th>
          <th>작성시간</th>
        </tr>
      </thead>
      <tbody id="postBody"></tbody>
    </table>
  </article>

  <nav aria-label="Page navigation example" class="pagination-container">
    <ul class="pagination"></ul>
  </nav>
</body>

</html>